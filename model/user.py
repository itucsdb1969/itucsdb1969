class User:
    def __init__(self, user_id, name, password, is_active, is_admin):
        self.user_id = user_id
        self.name = name
        self.password = password
        self.is_active = is_active
        self.is_admin = is_admin

